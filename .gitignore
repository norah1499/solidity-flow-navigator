__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.ruff_cache/
.DS_Store
Thumbs.db
.claude/

# Generated by serve.app at server startup; not source.
src/solidity_flow_navigator/serve/static/css/pygments.css

# Local working notes; deliberately not part of the published repo.
HANDOFF.md

# Design handoff bundle + brief for the index redesign — local reference material,
# not source (prototype HTML/JS + screenshots; reimplemented in the real stack).
design_handoff_index_redesign/
DESIGN_BRIEF.md

# Installed coding-assistant skills (e.g. `npx skills add`); local tooling, not source.
.agents/
skills-lock.json

# Internal working docs — kept on disk for development, not part of the public repo.
CLAUDE.md
solidity-flow-navigator.md

# Local config written by the tool (Save bindings); a per-checkout runtime artifact, not source.
solflow.toml
